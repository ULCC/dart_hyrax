<%= f.input key,
            as: :multi_value_people,
            input_html: {
                class: 'form-control',
                data: {'autocomplete-url' => "/authorities/search/local/people",
                       'autocomplete' => key},
            },
            required: f.object.required?(key)
%>
