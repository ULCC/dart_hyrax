<%= f.input key,
            as: :multi_value_autosuggest,
            input_html: {
                class: 'form-control',
                data: {'autocomplete-url' => "/authorities/search/local/current_people",
                       'autocomplete' => key},
            },
            required: f.object.required?(key)
%>
