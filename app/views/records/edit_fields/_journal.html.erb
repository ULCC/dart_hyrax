<%= f.input key,
            as: :multi_value,
            label: 'Journal',
            input_html: {
                class: 'form-control',
                data: {'autocomplete-url' => "/authorities/search/crossref/journals",
                       'autocomplete' => key},
            },
            required: f.object.required?(key)
%>
